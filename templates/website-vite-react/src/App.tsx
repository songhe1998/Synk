import "./styles.css";

export default function App() {
  return (
    <main className="app-shell">
      <section className="starter-hero">
        <p className="eyebrow">Skratch website starter</p>
        <h1>Replace this with a sketch-faithful website, not a generic starter card.</h1>
        <p className="body-copy">
          This workspace gives you a lightweight Vite + React + TypeScript baseline. Rewrite it completely if needed.
        </p>
      </section>
    </main>
  );
}
